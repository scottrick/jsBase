/* 
	System

	A system is something that operates on entities that contain the required components.
*/

/* types should be a list of component types */
var System = function(types) {
	this.types = types;
};
